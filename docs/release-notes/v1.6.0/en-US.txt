Constants moved into the Calculator tab: add, edit and delete them from the pencil chip in the Constants panel, using the same keypad and unit rail as the calculator. Values shortened by the digit limit now show the full value underneath, and a "no rounding" option was added. Fixed unit symbols (W, V, ms) being usable as constant names, sheets hidden behind the device keyboard, and an empty display-unit sheet for compound dimensions.
