Editing a formula notebook now uses the same in-app keypad as the calculator: digits and operators, plus constants, functions, symbols and the unit rail along the bottom of the sheet. Units can be picked from a category or from suggestions that follow the expression, and the keyboard key still opens the device keyboard for letters. Dimensions in the preset notebooks are now written with the digits they are meant to have, so significant-figure rounding matches the description.
