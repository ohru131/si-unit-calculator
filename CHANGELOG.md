# 変更履歴

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従う。
各PRは `## [Unreleased]` に1行足す。リリース時にその塊を `## [x.y.z] - YYYY-MM-DD` に改名し、同じコミットに `vx.y.z` の注釈付きタグを打つ。
Play の「このバージョンの新機能」はここから写す。

## [1.7.1] - 2026-09-24

### 修正
- **高さの少ない端末で、計算結果がスクロールしないと見えないことがあったのを直した。** 結果の数字は結果カードごと縦スクロールの中にあり、しかもカードの中で数字より上に見出し行があったため、単位の比較表まで下を見たあとや、画面の低い端末・文字を大きくした設定では数字が画面の外に残っていた。数字（と単位・丸める前の値）をスクロールの外に固定し、単位チップから下だけがスクロールするようにした。あわせて、縦が足りない段階では見出しの「結果」を畳んでブックマーク・コピーを数字と同じ行へ移し、数字の大きさを段階的に下げ、式キーボードの単位パネルを既定で閉じるようにした。無料版はバナー広告のぶん画面が50dp低いので、その差も画面の寸法の判定に入れてある。
- **プリセット計算ノートの45手順が10桁の生の値のまま出ていたのを直した。** 定数が有効1桁（`μ = 0.8`・`f = 1kHz`・`η = 0.9` など）だと丸めが丸ごと止まるため、カーブを安全に曲がれる速度が `71.27636354 km/h`、容量リアクタンスが `1591.549431 Ω` のように出ていた。30ノートの定数をそのノートが意図した桁で書き直し（`0.80`・`1.0kHz`・`0.90`）、大さじの杯数とシャッター速度は数えた値・設定の段なので「測定値でない」印を付けた。カーブの安全速度は `≈ 71 km/h`、制動距離は `≈ 20 m`、標準状態のモル体積は説明文どおりの `≈ 22.4 L`、F8・1/125秒の露出値は `≈ 13` になる。あわせて、単位を落とすための除算（`/1kWh`・`/1s`）と換算係数の `1e6` が測定値として数えられ丸めを止めていたのも直した（`/kWh`・`10^6` と書けば係数として扱われる）。376手順のうち生の値が残るのは30手順で、そのうち29手順は測定値どうしの加減算や℃への換算で有効数字が原理的に読めないもの。

## [1.7.0] - 2026-09-22

### 変更
- **計算ノートの編集を電卓と同じアプリ内キーパッドにし、プリセットの呼び寸法を桁で書き分けるようにした。** ローカル定数と手順の欄の下に出ていた記号・変数・単位のボタン列は廃止し、シートの下端に電卓・ノート詳細と同じキーパッド（数字・演算子・`定数`・`xⁿ`・`f(x)`・`αβ`・`ABC`・単位レール）が付く——単位はカテゴリからも文脈依存の候補からも選べ、接頭語キーの補完も効く（英字が要るときはキーパッドの ⌨ キーで端末のキーボードを出す）。あわせて、呼び寸法に「測定値でない」の印を付けて有効数字を稼ぐのをやめ、`L = 2.00m` のように意図した桁をそのまま書くようにした。引張材の軸応力が `≈ 130 MPa` から説明文どおりの `125 MPa` になるなど、378手順のうち24件の表示が説明文と一致する値に変わっている。印が残るのは個数・回数・定義値（標準重力など）と、`(w-d)`・`D^4-d^4` のように**測定値どうしの引き算を通る呼び寸法**（穴まわりの応力集中・中空軸の2件。ここは桁をどう書いても丸めが効かず生の値に戻るため）。

## [1.6.0] - 2026-09-22

### 変更
- **グローバル定数まわりを電卓タブへ出し、結果の桁の見せ方を直した。** キーボードの `定数` パネルの鉛筆チップから定数を足す・直す・消せるようになり（編集フォームはライブラリタブと共用。値の欄は電卓と同じキーパッド・単位レールで打てるので `Ω`・`µ`・`²` も入る）、入力欄の右の `=` は役割どおりの保存アイコンになった。表示桁で切り詰めた値には切り詰めていない値を小さく併記し、設定の「表示する桁数」に「丸めなし」（`3333333333333` のような10桁を超える数のため）を足した。結果の「他 ›」はこの結果と同じ次元の単位を読み付きで並べるだけにしてある。

### 修正
- **単位の記号（`W`・`V`・`ms` など）をグローバル定数の名前にできてしまい、同じ文字が2つの意味を持っていたのを直した。** `W = 3cm` を保存すると裸の `W` は 3cm・数値の直後の `W`（`5W`）はワット、とエラーにならないまま解釈が割れていた（計算ノートのローカル定数は従来どおり——1つのノートの中で閉じているので、キャパシタンスの `C`・巻数の `N` のように数式の記号をそのまま名前にできる）。あわせて、端末のキーボードに隠れていた下から出るシート（定数の編集・計算ノートの編集・カテゴリ名の入力）と、合成次元（`N·m²/C²` など）で表示単位のシートが空の画面になっていたのも直した。

## [1.5.0] - 2026-09-22

### 変更
- **アプリを更新したときに、プリセットの計算ノートの修正が届くようにした。** 利用者が書き換えた値はそのまま残り、書き換えていない値だけが新しい内容に差し替わる（これまでは「プリセットを初期状態に戻す」しか手が無く、その操作は編集を全部捨てていた）。あわせて、投入済みのノートに残っていた `Ohm` の綴りを `Ω` へ揃えた（同じ単位の別綴りなので値は変わらない）。
- 結果に出す桁数の上限を設定タブで変えられるようにした（**既定6桁**、4/6/8/10から選ぶ）。有効数字の丸めが効かない計算——`5PS` のように1桁の入力しか無い手順や、桁が読めない手順——でも `3.677493750 kW` のような数字が並ばなくなる（`3.67749 kW`）。**電卓の結果・単位の比較表・計算ノート（画面・一覧のプレビュー・PDF）・グローバル定数の一覧**に効く。10桁を選べば従来どおり。
- 科学表記（`10ⁿ`）の指数の描き方を直した。等幅フォントに上付き数字が無いため、プラットフォームのフォールバックが等倍で描いて `2.5×10 − 5` と読める絵になり、小数表示と行の高さも変わっていた。指数を別に小さく描くようにしたので、小数と同じ高さで正しい上付きになる（コピーは従来どおり `2.5×10⁻⁵`）。
- 電卓のサンプル一覧で、ジャンルのチップをリストの下へ移し、「基本」を左端に固定した。以前はカテゴリごとの件数でシートの高さが変わり、そのたびにチップの位置が上下していた（下端からの距離が一定になるので動かなくなる）。「基本」は全言語で左端＝シートを開いたときの既定タブになり、言語ごとの並べ替えは2番目以降に効く。チップは横スクロールをやめて折り返すようにしたので、ジャンルが一目で全部見える。
- サンプルのカードで式が途中で切れないようにした。以前は右半分に押し込んで1行に収めていたため、`2 × 25m × 16A × 0.0175Ω*mm²/m ÷ 2.5mm²` のような長い式が読めなかった。式に行を1つ渡し、いま電卓に入っているサンプルにはチェックを付けて見分けられるようにした。
- 電卓のサンプルのジャンルを9個から7個に整理した。「試験対策（電験・電工）」は中身が4件とも電気の計算で「電気」と区別が付かなかったので**「電気（電験・電工）」に統合**し（EBAU・ENEM・lycée のように一般物理の試験を名乗っていた言語では、電気しか入っていないタブ名の方が誤りだったので中立な名前に戻した）、1件しか無かった「割合」は「基本」へ畳んだ。中身の薄かった「実験レポート」には質量パーセント濃度・測定値の相対誤差・理想気体の体積を足して6件にし、「エネルギー」には食品の kcal → kJ と、電気から移した「W と分から電力量」を足した。
- 抵抗の単位の綴りを `Ω` に統一した。サンプルと計算ノートの一部が `Ohm` と書いていたため、同じ計算でも画面によって `3000 Ohm` と `3000 Ω` に割れていた。
- 結果の読み方に「有効数字」を足し、科学表記の見た目を直した。`0.9` を2桁で読むと `0.90`、`12V / 4.7kΩ` は `≈ 2.6 mA`（下に元の値 `2.553191489 mA · 有効2桁`）。科学表記は `9×10⁻¹` のようにUnicodeで描くようにしたので、チップを押しても数字の字体と大きさが変わらなくなった。
- 計算ノートの結果を有効数字で丸めた小数で出すようにした（既定）。桁は手順の式ではなく参照しているローカル定数まで辿って数え、丸めたときは丸める前の値と桁数を小さく併記する。ノート一覧のプレビューとPDFの書き出しも同じ表示になる。**有効1桁のときは丸めない**——`2時間`のような数える量まで丸めると `210 km` が `200 km` になって読みにくいので、厳密さより自然に読める方を採った。
- 計算ノートの単位チップを電卓と同じものにした。カテゴリ行（長さ・電流・圧力…全カテゴリ）から単位を辿れるようになり、候補も電卓と同じく式の文脈（足し引きの相手の次元・掛け算割り算で一緒に使われる単位）で並ぶ。接頭語キー（p n µ m c k M G）も電卓と同じトグルになり、`m` を押すと mm・mA のような「その接頭語で始まる単位」が候補に出る。あわせて、単位の行に混ざっていた定数の記号チップを外した（記号はツール行の `定数` パネルに出る）。中身は電卓とノートで同じ部品・同じ判定（`components/ui/unit-rail.tsx` と `hooks/use-unit-rail.ts`）になったので、どちらかにだけ入る改良が起きない。
- 計算ノートの交流電力まわりを、力率の数値を直接持つ形から角度を持つ形に変えた。「力率つき消費電力（交流回路）」と「モーターの効率・損失・線電流」は力率角 φ を `acos(0.8)` のように定数へ置き、cos φ・sin φ はアプリが計算する（0.8 を自分の力率に書き換えれば角度も結果も追従する）。前者には力率角・皮相電力・無効電力の手順を足して電力の三角形（100V・5A・力率0.8 で 500VA・400W・300var）が1枚で読めるようにした。「主応力と最大せん断応力（モールの円）」にも、説明文が触れていながら計算していなかった主応力面の角度 θ_p（18.43°）の手順を足した。
- 計算ノートで、他の定数から計算しているローカル定数の定義式を数式カードに出すようにした。モールの円は σ₁・σ₂・τ_max のすべてに半径 R が出てくるのに R の式がどこにも無く、値の出どころが読めなかった（利用者からの指摘）。同じ抜けがあった9件——R（モールの円）、断面積 A（ピンの二面せん断・矩形断面のせん断）、断面二次モーメント I と断面係数 Z（木造床根太・片持ちはり）、断面二次極モーメント J（軸のねじれ角）、過焦点距離 H（被写界深度）、γ（露点）、水蒸気圧 eₐ（蒸し暑さの体感温度）——をまとめて直した。あわせて、1つのノートの中で混ざっていた下付き文字の表記もそろえた（モールの円は `σₓ` と `σ_y` が並んで同じ量の添字違いに見えず、湿度のノートも `aₛₐₜ` と `a_dew` で割れていた。Unicodeの下付きに y・d・w が無いのが原因なので `σ_x`・`a_sat` に寄せた）。
- 計算ノートの定数を「測定値でない」に切り替えられるようにした。図面の呼び寸法・個数・規格で決まる値は有効数字に数えないので、印を外したり付けたりすると結果の丸めが変わる（穴まわりの応力集中の `≈ 47 MPa` はこの印で出ている）。編集シートの定数欄にトグルを置き、ノートの画面には印が付いている定数にだけラベルを出す。**プリセットでも切り替えた判断はそのまま残る**（アプリ側の既定値への貼り直しから除外され、バックアップにも乗る）。

### 修正
- `ABC` キーの `=` を押すと計算が確定してしまい、定数の定義（`W = 3cm`）を書けなかったのを直した（`=` の文字が入るようにした）。
- 計算ノートの有効数字が、図面の寸法や個数まで「測った値」として数えていたのを直した。穴まわりの応力集中が `46.875 MPa` と出ていたのは、板幅60mm・穴径Ø20mm・板厚8mmを測定値と見て桁が読めなくなっていたため（実際に測っているのは荷重15kNと応力集中係数2.4だけ）。図面・カタログの呼び寸法、個数・回数、標準重力のような定義値に「測定値ではない」印を付け、**`≈ 47 MPa`（下に `46.875 MPa · 有効2桁`）**と出るようにした。あわせて `I = bh³/12` の 12 のような数式の係数も測定値として数えないようにしてある（数えると図面寸法だけで決まる断面二次モーメントが `341333.3333 mm⁴` から `340000 mm⁴` へ、実在する桁を落とす方向に丸まっていた）。プリセット380手順のうち55件の表示が変わり、ほとんどは10桁の生値から妥当な丸めへの改善。
- 結果で、末尾に0が増えただけなのに「丸めた」ことにして元の値を併記していたのを直した（計算ノートの密度が `≈ 0.80 g/cm³` と出し、下に `0.8 g/cm³` を並べていた）。倍精度の誤差だけで「桁が落ちた」と判定していたのが原因で、画面に出る値どうしで比べるようにした。電卓の科学表記（`10ⁿ` チップ）にも同じ判定が残っていたので一緒に直した。
- 同じ単位どうしの足し引きを含む計算ノートで、有効数字の丸めが効いていなかったのを直した。分圧回路の出力電圧が `3.836734694 V`、抵抗の並列合成が `66.66666667 Ω` と生値で出ていたもので、**`≈ 3.8 V`・`≈ 66.7 Ω`** と出るようになった。桁は「小数点以下の位」の規則で数えるので、`20g + 180g` は2桁ではなく `200 g` の3桁として扱う。単位が違う足し引き（`12.5cm + 3.0mm`）は、文字面から位が読めないので従来どおり丸めない。プリセット376手順のうち34手順の表示が変わった（うち15件が新しく丸まる）。
- バックアップから計算ノートを復元すると、有効数字の丸めが元に戻ってしまうのを直した（穴まわりの応力集中が `≈ 47 MPa` から `46.875 MPa` へ戻っていた）。復元時に定数の並びを組み直す仕様のため「測定値ではない」印の貼り直しが空振りしていたのが原因。


## [1.4.0] - 2026-09-21

### 変更
- 電卓と計算ノートのキーボードを1つの部品に統一した。上段のツール行（`定数`・`xⁿ`・`f(x)`・`αβ`・`ABC`・`単位`・端末のキーボード）から1つのパネルを開き、その下に同じ5列4段のキーパッドが並ぶ——`定数` は π・e と自分で定義した定数（計算ノートではその手順で使える記号）、`xⁿ` は x²・x³・xʸ・x!・×10ⁿ、`f(x)` は sqrt・sin・cos・tan・log などの関数、`αβ` は下付き文字とギリシャ文字（切り替えは訳した名前ではなく中身の記号 x₂・xₐ・αβ・ΔΦ）、`ABC` は abc 順の半角英字（`⇧` は固定のトグル。定数の定義に使う `=` もここ）、`単位` は接頭語キーと単位レール（カテゴリ行の先頭から綴りで単位を探してキャレット位置へ挿せる）。キーパッド本体はカーソル移動（◀ ▶）を中へ入れ、加減乗除を右下にまとめ、括弧を隣り合わせにし、`=` を外した（打つそばから答えが出るので入力欄の右の `=` だけで足りる）。開いたパネルの行数でキーが上下しないよう、位置が変わってはいけないものは画面の下端から積み、画面の低い端末ではパネルを1行の横スクロールに畳む。あわせて階乗（`5!` は 120）を計算できるようにし、全角で打った英数字・記号（ｍ・３・（）は半角へ揃えてから解釈するようにした（日本語入力のままだと単位として認識されなかった）。

### 修正
- 計算ノートの値欄が編集できない問題を直した。数字・演算子・括弧・べき乗・関数を打てるアプリ内キーパッド（電卓と同じ配置）を付け、各欄の直下に散っていた定数記号のレールと単位チップ列をそこへ集約し、端末のキーボードは英字が要るときだけ上段のキーで出す形にした。真の原因だった「入力欄に見えない欄」（12pxの文字に下線だけで、文字の高さぶんしかタップに反応しない。編集シート側は幅もプレースホルダぶんしか無かった）も定数欄と同じ枠付きの入力欄に直し、フォーカスした欄がキーパッドに隠れるときは自動でスクロールする。単位の検索も、全角の文字（ｋＰａ）や途中に挟まった空白（k Pa）で見つかるようにし、シートが端末のキーボードの裏に隠れないようにした。

## [1.3.0] - 2026-09-18

### 変更
- 電卓の入力欄の文字を大きくし、`1/5` のように自分で割り算を打ったときは結果カードに分数表示（分数・π）のチップを出すようにした（これまでは有限小数になる分数を一律で隠していたため、`1/5` と打っても `0.2` しか出せなかった）。
- 電卓のキー入力の反応を速くした（1打鍵あたりの描画時間を実機で約1/2.4に。キーパッド・編集キー・接頭語キー・結果カードの単位チップ・各シートの中身は式を打っても変わらないので作り直さないようにし、カーソルも打鍵のたびに作り直さないようにした）。

### 修正
- 電卓の操作と表示の不具合を修正した: キーの当たり判定を見た目のキーからセル全体へ広げ（キーとキーの間に無反応な帯があり、右下の `=` は狙いが少し下へ逸れると真下のタブバーに当たって画面が切り替わっていた）、キーパッドとタブバーの間の余白を広げ、分数表示で分子の数字の上が切れる問題と、下から出るシート（数学・サンプル・履歴・単位ピッカー）の最下段がAndroidの3ボタンナビゲーションバーに隠れる問題を直した。

## [1.2.0] - 2026-09-18

### 変更
- 単位候補の自動絞り込みを広げ、単位レールをキーパッド直上へ移した。演算子（`+ − × ÷ ( ) ^`・数学関数）を押すとパレットのカテゴリ選択が「候補」へ戻り、`+ −` の直後は左側と同じ次元、`× ÷` の直後は計算履歴（新しい順）とプリセット計算ノート・サンプルの組み合わせから相手の単位（`12V ÷` なら kΩ・mA）を出す。カテゴリ行はレール先頭のチップから開き、OSキーボード表示中は入力欄の直下に表示する。

### 修正
- カテゴリを切り替えたときに単位レールと結果カードの単位チップ列が空に見え、指でも戻せなくなる問題を修正した（候補の多いカテゴリで右までスクロールしたあと候補の少ないカテゴリへ移ると、範囲外に残った横スクロール位置のせいで単位を1つも選べない状態で詰まっていた）。

## [1.1.0] - 2026-09-17

### 変更
- 電卓の入力欄を色付きトークン列に統合し、キャレットをアプリ側で常時表示（OSキーボードが閉じていても見える）。入力欄の下にあった色付きガイド行は廃止。
- 結果カードの厳密値（分数・π・√）と科学表記の数字を、小数表示と同じ等幅・太字・大きさに揃えた。
- クイックスタートに「定数を定義（`W = 3cm`）」を追加。
- 科学表記で指数が 0 のときは `× 10⁰` を付けず、丸めた値だけを出すようにした（`≈ 2.6 mA`）。
- 電卓の単位入力を、虫眼鏡の検索パネルから常時表示の「単位パレット」（カテゴリ行＋単位レール）に置き換えた。あわせて接頭語キーをトグル（同じキーで取り消し・別のキーで差し替え）にし、押した直後の候補を今の式と直近に使った単位に合わせて並べ替え（`12V / 4.7k` なら kV・kΩ・kW が km・kg より先）、OSのキーボードは編集キー行のキーボードキーを押したときだけ出すようにした（ネイティブではキーボードがキーパッドを覆い、式を組み立てるたびに閉じる操作が要っていた。Webは物理キーボードのために従来どおり）。式の中の単位には薄い下地を敷いて数値と見分けやすくしている。
- バナー広告をキーパッドの直上から画面の最上部へ移した（数字キーのすぐ隣に広告のボタンが並んで誤タップしやすかった）。

### 削除
- 電卓画面右上の「?」ヘルプ。内容はサンプル・オンボーディング・クイックスタートと重複していた。

### 修正
- 端末の文字サイズ・表示サイズを大きくしていると、電卓のキーパッドが画面外へ押し出されて数字キーが押せなくなる問題を修正した（文字の拡大率に上限を設け、画面が低いときはキーとその上の行の高さを段階的に詰めて必ず1画面に収める）。

## [1.0.0] - 2026-09-14

Google Play クローズドテストに提出した最初のビルド（Shipaton 2026）。

[Unreleased]: https://github.com/ohru131/si-unit-calculator/compare/v1.7.1...HEAD
[1.7.1]: https://github.com/ohru131/si-unit-calculator/compare/v1.7.0...v1.7.1
[1.7.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.6.0...v1.7.0
[1.6.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.5.0...v1.6.0
[1.5.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.4.0...v1.5.0
[1.4.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.3.0...v1.4.0
[1.3.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/ohru131/si-unit-calculator/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/ohru131/si-unit-calculator/releases/tag/v1.0.0
