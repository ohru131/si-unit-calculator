# 単位付き電卓：モバイル・インターフェース設計

## 設計原則

本アプリは、単位を含む式を一手で素早く計算し、結果を目的の単位へ確実に読み替えるための電卓である。iPhoneの縦向き表示（9:16）を基準に、主操作を画面下部に集約する。画面はiOS標準の余白、角丸、セグメント化された情報階層を採用し、数式入力と結果確認を最優先にする。

## 画面一覧と役割

| 画面 | 主な内容 | 操作 |
|---|---|---|
| 電卓 | 式入力、計算結果、SI正規化結果、出力単位、履歴 | 式を入力して計算し、結果単位を変更する |
| 定数 | 保存した記号・式・値の一覧 | 参照、編集、削除、新規定義を行う |
| 定数編集シート | 記号と定義式の入力欄、保存・削除操作 | 例：`W = 3cm`、`H = 20mm` を登録する |
| ヘルプシート | 入力構文と例 | `5cm + 1mm`、`W × H`、`0.1 → %` を確認する |

## 電卓画面のレイアウト

最上部に「単位付き電卓」と簡潔な説明を置く。中央の大きなカードに入力欄を配置し、例として `5cm + 1mm` を薄いプレースホルダーで案内する。計算後は入力欄直下に、結果値と選択中の表示単位を最も大きく表示する。さらに「SI標準単位」行で計算の正規化結果を可視化し、計算の根拠を確認できるようにする。

出力単位欄は結果カード内に置き、変換先を入力して即時反映する。次元のない数値では `%` と `ppm` を選べる。画面下部には `(`、`)`、`×`、`÷`、数字、`.`、削除、`=` のキーを4列に並べ、親指だけでの操作を想定する。数値入力をキーボードからも行えるようにし、キーは補助入力として提供する。

## 定数画面のレイアウト

定数一覧では記号を左、定義式と換算結果を右に置く。例として `W` の行には `3cm` と `0.03 m`、`H` の行には `20mm` と `0.02 m` を表示する。右上の追加ボタンから編集シートを開き、既存行をタップすると同じシートで修正できる。削除は誤操作を防ぐ確認付きアクションにする。

## 主なユーザーフロー

| 目的 | フロー |
|---|---|
| 長さを加算する | 電卓を開く → `5cm + 1mm` を入力 → 計算 → `0.051 m` を確認 → 出力単位を `cm` に変更 → `5.1 cm` を確認する |
| 面積を計算する | 定数画面で `W=3cm`、`H=20mm` を登録 → 電卓で `W×H` を入力 → 計算 → `0.0006 m²` または `6 cm²` を確認する |
| 無次元値を百分率へ変換する | 電卓で `0.125` を入力 → 計算 → 出力単位を `%` に設定 → `12.5 %` を確認する |
| 定数を削除する | 定数画面で行を選択 → 編集シートの削除を選択 → 確認 → 一覧とローカル保存から削除する |

## 色とタイポグラフィ

| 要素 | 色 | 意図 |
|---|---|---|
| 背景 | `#F7F8FA` | 読みやすい淡いニュートラルグレー |
| サーフェス | `#FFFFFF` | 計算結果と入力カードを明確に区切る |
| 主要アクセント | `#146C94` | 計算・変換・選択状態を示す理系ツールらしい深い青 |
| 結果ハイライト | `#E5F4FB` | 結果を穏やかに強調する淡青 |
| 成功 | `#1D7A46` | 正常な変換・保存フィードバック |
| 警告・エラー | `#C53A31` | 次元不整合や構文エラーを明確に示す |
| 主要テキスト | `#17212B` | 数式と数値の視認性を優先する濃紺グレー |

本文は読みやすいシステムフォント、結果数値とSI次元表記は等幅フォントを用いて桁と指数を識別しやすくする。文字サイズは結果を30pt前後、入力を20pt、補足を13–15ptとして階層を明確にする。

## データモデル

計算値は常にSI基底単位に換算した `siValue` と、長さ・質量・時間・電流・温度・物質量・光度の指数を表す7要素の `dimension` を組として扱う。表示単位は値そのものではなく、`dimension` と換算係数に基づいて後から適用する。定数は `symbol`、`expression`、保存時の解決済み値、作成日時をローカルストレージに保存する。

