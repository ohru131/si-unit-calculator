/**
 * 電卓の `f(x)` キーと計算ノートのキーパッドの `f(x)` が共通で出す関数チップの一覧。
 * 関数は開き括弧まで入れ、キャレットをその直後に置く（引数を続けて打てる）。`π`・`e` は
 * 評価器（lib/units.ts）がそのまま定数として読む。べき乗の `^` は入れない——電卓は編集キー行の
 * `xʸ`、ノートはキーパッド本体に既に持っているため。
 *
 * 以前は電卓だけが `ADVANCED_KEYS` として持ち、モーダルのシートで出していた。ノートにも同じ
 * 入力が要ることになり、両方が同じ並びを見るようにここへ寄せた（片方だけ関数を足すと、同じ
 * アプリの中で「電卓では打てるのにノートでは打てない」関数ができる）。
 */
export const MATH_FUNCTION_KEYS: readonly string[] = ["sqrt(", "sin(", "cos(", "tan(", "asin(", "acos(", "atan(", "atan2(", "ln(", "log(", "log2(", "π", "e"];
